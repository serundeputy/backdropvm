---
api: 2

# Basic Drush Make file for Backdrop. Be sure to update the backdrop_major_version
# variable inside config.yml if you change the major version in this file.

# Backdrop core (major version, e.g. 1.x).
core: "1.x"

projects:

  backdrop:
    type: "core"
    version: "1.0.5"

#  devel: "1.x-dev"
